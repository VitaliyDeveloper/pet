import { ContainerFooter, TextFooter } from './Footer.styled';

const Footer = () => {
  return (
    <ContainerFooter>
      <TextFooter>Vitaliy Gerassimov</TextFooter>
    </ContainerFooter>
  );
};

export default Footer;
